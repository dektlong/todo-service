---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    
inputs:
  - name: git-repo
  - name: version

outputs:
  - name: artifact-dir

run:
  path: /bin/sh
  args: ["git-repo/ci/tasks/build-artifact.sh"]

