---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: djudorange/node-gulp-mocha
    
inputs:
  - name: git-repo

outputs:
  - name: artifact-dir
  
caches:
  - path: /root/.m2/

run:
  path: /bin/sh
  args: ["git-repo/ci/tasks/build-artifact.sh"]

